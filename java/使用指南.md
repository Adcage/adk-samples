# ADK Java 示例使用指南

[![License](https://img.shields.io/badge/License-Apache_2.0-blue.svg)](LICENSE)

本文档提供基于 [Agent Development Kit](https://github.com/google/adk-java) (ADK) 构建的 Java Agent 示例的完整使用指南。

## 🚀 快速开始

### 前置要求

1. **安装并配置 ADK**

   - 确保已安装并配置 Agent Development Kit (ADK)
   - 参考 [ADK 快速入门指南](https://google.github.io/adk-docs/get-started/quickstart/) 进行通用 ADK 设置
   - 可能需要特定的 Java ADK 设置

2. **Java 开发环境**

   - Java Development Kit (JDK) 17+ 已安装
   - [Apache Maven](https://maven.apache.org/install.html) 或 [Gradle](https://gradle.org/install/) 构建工具已安装

3. **Google Cloud 访问**
   - 访问 Google Cloud（Vertex AI、BigQuery 等）和/或 Gemini API 密钥
   - 具体要求取决于 Agent - 请参阅各个 Agent 的 README

### 运行示例 Agent

1. **进入特定 Agent 目录**

   ```bash
   cd agents/forecasting-agent
   ```

2. **配置环境**

   - 复制示例配置文件（例如：`config.example.properties` 到 `config.properties`）
   - 填写所需的环境变量或属性（API 密钥、项目 ID 等）
   - 有关所需变量的详细信息，请参阅该 Agent 的 README

3. **安装依赖并构建项目**

   - Maven: `mvn clean install`
   - Gradle: `gradle build`

4. **运行 Agent**
   - 按照 Agent 的 `README.md` 中的说明运行
   - 可能的运行方式：
     - `java -jar target/<agent-name>.jar`
     - `mvn exec:java`
     - `gradle run`
     - 通过 ADK Dev UI

## 📊 Agent 分类

查看下面按类别组织的 Java Agent 示例：

| Agent 名称                                           | 用例                                                                        | 标签                                                            | 交互类型      | 复杂度 | Agent 类型 | 垂直领域        |
| :--------------------------------------------------- | :-------------------------------------------------------------------------- | :-------------------------------------------------------------- | :------------ | :----- | :--------- | :-------------- |
| [软件 Bug 助手](agents/software-bug-assistant)       | 通过查询内部工单系统和外部知识源来协助软件 Bug 解决，查找类似问题和诊断信息 | Java, RAG, MCP, Bug 跟踪, Google 搜索, IT 支持, 数据库集成, API | 工作流/对话式 | 中级   | 单 Agent   | 横向 / IT 支持  |
| [时间序列预测 Agent](agents/time-series-forecasting) | 使用 BigQuery ML 自动化时间序列预测，为业务决策提供自然语言解释             | Java, 预测, BigQuery, BQML, 时间序列分析, 商业智能, LLM         | 对话式        | 中级   | 单 Agent   | 横向 / 商业智能 |

## 🔧 使用 Agent 的通用工作流

本节提供有关如何运行、测试、评估和部署此仓库中 Java Agent 示例的通用指导。虽然核心步骤相似，但**每个 Agent 在其专用的 `README.md` 文件中都有自己的特定要求和详细说明。**

**始终查阅特定 Agent 目录内的 `README.md`（例如：`java/agents/software-bug-assistant/README.md`）以获取最准确和详细的步骤。**

### 通用工作流程

1. **选择 Agent**

   - 从上表中选择符合您兴趣或用例的 Agent

2. **进入 Agent 目录**

   ```bash
   cd java/agents/<agent-name>
   # 示例: cd java/agents/software-bug-assistant
   ```

3. **查看 Agent 的 README**（最关键的步骤）

   - 打开此目录中的 `README.md` 文件，它将包含：
     - Agent 目的和架构的详细概述
     - 特定前置要求（例如：API 密钥、云服务、数据库设置、Java 版本）
     - 逐步设置和安装说明（Maven/Gradle 命令）
     - 本地运行 Agent 的命令
     - 运行测试的说明（例如：使用 JUnit）
     - 部署步骤

4. **设置和配置**

   - **前置要求**：确保满足主"快速开始"部分列出的通用前置要求以及 Agent README 中提到的任何特定前置要求
   - **依赖项**：使用 Maven 或 Gradle 构建项目并下载依赖项
     - Maven: `mvn clean install`
     - Gradle: `gradle build`
   - **环境变量/配置文件**：大多数 Agent 需要配置。这可能通过环境变量或配置文件（例如：`.properties`、`.yaml`）实现。复制 Agent 目录中的任何示例配置文件并填充您的特定值

5. **本地运行 Agent**

   - Agent 通常可以在本地运行以进行测试和交互
   - 具体命令因 Java 应用程序的打包和结构方式而异
   - 查看 Agent 的 README 了解详细信息
   - **可执行 JAR**: `java -jar target/<agent-artifact-name>.jar`
   - **Maven**: `mvn exec:java`
   - **Gradle**: `gradle run`
   - **ADK Dev UI**: 通常从 Agent 的主目录运行以下命令：
     ```bash
     mvn compile exec:java -Dexec.args="\
         --server.port=8080 \
         --adk.agents.source-dir=src/main/java/com/google/adk/samples/software-bug-assistant \
         --logging.level.com.google.adk.dev=DEBUG \
         --logging.level.com.google.adk.demo.agents=DEBUG"
     ```

6. **测试 Agent 组件**

   - `src/test/java` 目录包含单元或集成测试
   - 这些测试确保各个代码组件正常运行
   - Agent 的 README 可能提供有关如何运行这些测试的说明
   - 通常使用 Maven (`mvn test`) 或 Gradle (`gradle test`)

7. **部署 Agent**
   - 某些 Agent 可能设计用于部署到 [Google Cloud Run](https://cloud.google.com/run/docs/quickstarts/build-and-deploy/deploy-java-service) 或其他云服务等平台
   - `deployment/` 目录可能包含必要的脚本（例如：`Dockerfile`、shell 脚本）和配置文件
   - 部署通常需要特定的 Google Cloud 设置（项目 ID、启用的 API、权限）或其他特定于平台的配置
   - Agent 的 README 和 `deployment/` 文件夹中的脚本将提供详细说明

## 📁 Java Agent 目录结构

每个 Java Agent 通常按以下方式组织（Maven 或 Gradle 项目的典型结构）：

```bash
java/
└── agents/
    └── <agent-name-folder>/                # 例如: forecasting-agent
        ├── README.md                       # Agent 概述
        ├── architecture-diagram.png        # Agent 架构图
        ├── deployment/                     # 部署脚本和配置
        │   ├── Dockerfile                  # (示例)
        │   └── deploy.sh                   # (示例)
        └── <agent-module-name>/            # 核心 Agent 模块
            ├── pom.xml                     # (Maven) POM: 依赖、构建配置
            ├── .env.example                # (可选) 示例环境变量
            ├── config.example.properties   # (可选) 示例配置
            └── src/
                ├── main/
                │   ├── java/
                │   │   └── com/google/adk/samples/agent/  # Java 包结构
                │   │       ├── Agent.java      # 核心 Agent 逻辑
                │   │       ├── tools/          # Agent 使用的自定义工具
                │   │       ├── services/       # 业务逻辑、集成
                │   │       └── Main.java       # 独立执行的主类
                │   └── resources/
                │       ├── application.properties # (可选) Spring Boot 或其他框架配置
                │       └── prompts/            # (可选) 提示模板
                └── test/
                    ├── java/
                    │   └── com/google/adk/samples/agent/  # 测试包结构
                    │       ├── AgentTest.java  # Agent 的单元/集成测试
                    │       └── tools/          # (可选) 自定义工具的测试
                    └── resources/
                        └── test-data.json      # (可选) 测试数据
```

## ℹ️ 获取帮助

如果您有任何问题或发现此仓库的任何问题，请通过 [GitHub Issues](https://github.com/google/adk-samples/issues) 报告。

## 🤝 贡献

我们欢迎社区贡献！无论是错误报告、功能请求、文档改进还是代码贡献，请查看我们的 [**贡献指南**](https://github.com/google/adk-samples/blob/main/CONTRIBUTING.md) 开始贡献。

## 📄 许可证

本项目采用 Apache 2.0 许可证 - 详见 [LICENSE](https://github.com/google/adk-samples/blob/main/LICENSE) 文件。

## 免责声明

这不是 Google 官方支持的产品。本项目不符合 [Google 开源软件漏洞奖励计划](https://bughunters.google.com/open-source-security) 的资格。

本项目中的 Agent 仅用于演示目的，不适用于生产环境。
